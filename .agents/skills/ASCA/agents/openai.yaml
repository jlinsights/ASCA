interface:
  display_name: "ASCA"
  short_description: "Repo-specific patterns and workflows for ASCA"
  default_prompt: "Use the ASCA repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true