📌 완료된 tailwind 계획에 정지 헤더 + docs/CLAUDE.md 에 계획·동적클래스·개수 규약 추가 — `docs/superpowers/plans/2026-05-17-dynamic-tailwind-class-fix.md` 가 **체크박스 53개 전부 미완**인데 상단에는 `**For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development … to implement this plan task-by-task` 라는 **실행 지시**가 달려 있다. 그대로 실행하면 이미 적용된 리팩터를 되돌린다. 🔴**완료 보고서가 없어 코드로만 판정했다**: Task 1 산출물인 `components/cultural/_constants/color-classes.ts` 와 `__tests__/color-classes.test.ts` 가 **둘 다 실재**하고, Task 2~6 의 대상 5개 컴포넌트(`CulturalCalendar`·`CalligraphyHero`·`VirtualExhibition`·`ArtistPortfolioGrid`·`LearningHub`)에 **동적 클래스 보간이 0건**이다 — 파일이 사라진 게 아니라 존재하면서 0건이라는 것까지 확인했다. ⚠️**`exhibition-detail-mockup-port.plan.md` 는 건드리지 않았다 — 진짜 진행 중이다.** 형제 디렉토리 `~/Developer/Projects/ASCA-exhibition-port` 를 중복 클론으로 의심해 조사했더니 같은 원격(`jlinsights/ASCA`)의 **두 번째 작업 클론**이었고, `feat/exhibition-detail-port` 에 고유 커밋 21개 + 다른 3개 브랜치에 20개 + 미커밋 4건이 있었다. 계획을 stale 로 표시했으면 살아있는 작업을 "완료"로 오인하게 만들 뻔했다 — 이 사실을 문서에 경고로 남겼다. ✅**동적 Tailwind 규약을 함께 넣었다** — `bg-${color}` 같은 보간 클래스는 **Tailwind 가 리터럴만 스캔**하므로 빌드 산출 CSS 에 들어가지 않는다. 중요한 건 **검증을 소스 grep 으로 하면 안 된다**는 점이다(정적 grep 은 이 버그를 원리적으로 못 잡는다) — 컴파일된 CSS 로 확인해야 한다고 명시했다. 「개수를 적지 않는다」 절도 추가했다. 🔴**보고할 것: 이 문서는 ASCA 세션에 자동 로드되지 않는다.** 루트에 `CLAUDE.md` 도 `AGENTS.md` 도 없고 유일한 것이 `docs/CLAUDE.md`(추적됨, 264줄, 최종수정 2026-05-14)인데 **Claude Code 는 루트만 자동 로드한다.** 즉 방금 넣은 규약을 포함해 이 문서 전체가 실제 세션에 들어가지 않는다. 홈 `CLAUDE.md` 의 "각 프로젝트에 자체 CLAUDE.md 있음 → 자동 로드" 는 ASCA 에서 사실이 아니다. **루트 파일 신설은 매 세션 로드 내용을 바꾸는 결정이라 임의로 하지 않았다** — 별도 판단이 필요하다. 변경은 `docs/CLAUDE.md` + 계획 1개, **전부 마크다운이고 코드 변경 0건**임을 `git diff --name-only` 로 확인했다.
